import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks'
import Button from '~/components/atoms/Button.vue'

<Meta
  title="Components/Atoms/Button"
  component={Button}
  argTypes={{
    color: {
      control: { type: 'select', options: ['primary', 'warning', 'error'] },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Button },
  template: '<Button :color="color">{{label}}</Button>',
})

# Button

Buttons

## Default

<Canvas>
  <Story
    name="Default"
    args={{
      label: 'テストボタン',
      color: 'primary',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
